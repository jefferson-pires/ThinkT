<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.riyad.thinkt">

    <application
        android:name=".DAO"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NovaViagem" />
        <activity android:name=".NovoGasto" />
        <activity android:name=".MinhasViagens" />
        <activity android:name=".MeuPerfil" />
        <activity android:name=".MeusGastos" />
        <activity android:name=".Extras" />
        <activity android:name=".BuscarFotos" />
        <activity android:name=".BuscarPassagens"></activity>
    </application>

</manifest>